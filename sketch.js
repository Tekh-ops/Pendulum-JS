// much better debugging here: https://editor.p5js.org/


function setup() {
    createCanvas(windowWidth, windowHeight);        // create a canvas using the screen's dimensions

}

function draw() {
    background(255, 255, 255);                      // set the background color (RGB)
}
